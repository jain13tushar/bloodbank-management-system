spring:
  application:
    name: donor-service
  datasource:
    url: jdbc:mysql://localhost:3306/donardb
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      
server:
  port: 8081